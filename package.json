{
  "name": "app-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages\\server",
    "packages\\client"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
